/// Centralized brand asset paths so logos are referenced consistently.
class VistarAssets {
  VistarAssets._();

  /// Square "S" mark — used for loaders, watermarks, sidebar brand glyph,
  /// shimmer overlays, and corner accents.
  static const sMark = 'assets/images/logo.png';

  /// Horizontal wordmark — used on splash + login art panel only.
  static const wordmark = 'assets/images/logo_name.png';

  /// App / browser icon — fall back for any small launcher spot.
  static const appIcon = 'assets/images/app_icon.png';
}
